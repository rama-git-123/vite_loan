import Home from "../pages/home";
const MainContainer = ({ mainData }) => {
  return (
    <div className="main-container">{/* <Home mainData={mainData} /> */}</div>
  );
};
export default MainContainer;
